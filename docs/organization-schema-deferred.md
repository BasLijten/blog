# Organization schema: deferred

This audit item remains unresolved because the site currently has no approved
public telephone number or postal address. The site publishes an email address
and social profiles, but those values do not satisfy the requested complete
`Organization` schema. Add `contactPoint.telephone` and a `PostalAddress` only
after the owner approves the exact public values. Until then, do not fabricate
business identity data or publish incomplete organization markup.
